<h1>All Tags</h1>

<ul id="tags">
	<% @tag.each do |tag| %>
		<li>
			<%= link_to tag.title, article_path(article) ,
		class: 'tag_title', id: "tag_#{tag.id}" %>
				
		</li>
	<% end %>
</ul>

<%= link_to "Create a New Article", new_article_path, class:"new_article" %>
